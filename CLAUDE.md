# CLAUDE.md

## コスト最優先（すべての依頼で適用）

クレジット消費を抑えることを、速さや網羅性より優先する。迷ったら「安いやり方」を選ぶ。

### やらないこと（依頼されない限り）
- **サブエージェント / Agent ツール / Workflow / 並列ファンアウトを使わない。** トークンを数倍〜数十倍に増やす最大の要因。必要だと思ったら、実行せずに「これは並列化すると速いが高コスト」と一言添えて許可を求める。
- **新しいルーティン（定期実行）や PR 監視・自動チェックインを勝手に作らない。** 1 回の発火 = 1 ターン分の課金。作るのは明示的に頼まれたときだけ。
- **確認のための読み直しをしない。** 編集後にファイルを再読み込みして確かめる、テストを念のため 2 回走らせる、といった重複作業は省く。
- **頼まれていない調査・リファクタ・関連ファイルの巡回をしない。** 依頼された範囲だけをやる。

### やること
- **ファイルは必要な範囲だけ読む。** 全文 cat ではなく `sed -n '120,180p'` や `grep -n` で狙って読む。大きいファイルほど厳守。
- **独立したコマンド・ツール呼び出しは 1 メッセージにまとめて並列実行する。** 往復回数＝課金回数。
- **調べる前に、まず答えられるか考える。** 既に分かっていることを再確認するための検索・読み込みをしない。
- **Web 検索は根拠が必要なときだけ、1〜2 回に絞る。**
- **報告は短く。** 長い要約表・進捗の逐次実況・オプションの羅列はしない。結論と、変えたファイルだけ。

### ユーザー側の節約ポイント（聞かれたら案内する）
- 単純な修正は Opus ではなく Sonnet（`/model`）で十分。コスト差が大きい。
- 使っていないコネクタ（Adobe / Canva / Figma / Meta / HubSpot / Supermetrics など）をオフにすると、毎ターンのツール定義分の課金が減る。
- 話が長くなったセッションは、毎ターン全履歴が再送されて高くつく。用件が変わったら新しいセッションか `/clear`。

## プロジェクト
`index.html` 単体で動くシンプルなアラームアプリ。ビルド・依存なし。ブラウザで開いて動作確認する。
